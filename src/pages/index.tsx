// src/pages/index.tsx

import TodoList from '../components/TodoList'; // Imports the main TodoList component for rendering

// Home component serves as the main entry point for the application
const Home = () => {
  return <TodoList />; // Renders the TodoList component as the main content of the homepage
};

export default Home; // Exports the Home component as the default export for the index page
